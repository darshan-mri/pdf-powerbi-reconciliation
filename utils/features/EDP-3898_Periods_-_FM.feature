Given User logs into Power BI
And User selects the workspace
When User opens the report
Then the Period by default must be set to Current Period
And the Periods filter should display the periods in descending order